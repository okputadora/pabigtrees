import React from 'react'

const Confirmation = () => (
  <div className="confirmation-text" style={{ fontSize: 50, color: 'black' }}>
      The nomination has been approved! It is now in the tree database.
  </div>
)

export default Confirmation
